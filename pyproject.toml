[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "healthcare_readmission_prediction"
version = "1.0.0"
description = "Preventable Readmissions Risk Stratifier - Machine Learning, SHAP interpretability & EHR feature engineering on CMS HRRP & MIMIC-IV"
readme = "README.md"
authors = [
    { name = "Abdussatar", email = "satarabdus692@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Information Analysis"
]
